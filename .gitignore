# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Environment Variables (CRITICAL) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Google Cloud Credentials ──
# Prevents accidentally uploading your downloaded service account JSON key
*.json
!package.json
!package-lock.json
!tsconfig.json
!cors.json

# ── Build Outputs ──
dist/
build/

# ── Logs ──
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Prisma ──
*.db
*.db-journal
# (Note: Prisma generated client lives in node_modules, which is already ignored)

# ── Mac OS Files ──
.DS_Store
.AppleDouble
.LSOverride

# ── Editor/IDE specific files ──
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?